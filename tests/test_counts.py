from unittest import TestCase
# from p2.counts import count_by_letter, LetterCount


class Test(TestCase):

    def test_count_by_letter(self):
        pass
        # f1 = count_by_letter(['a', 'h', 'c'], ['happy', 'hula', 'henry', 'apples', 'hoopla'])
        # lc_list = [LetterCount('a', 1), LetterCount('h', 4), LetterCount('c', 0)]
        # self.assertTrue(f1 == lc_list)

    def test_count_by_letter_2(self):
        pass
        # f1 = count_by_letter(['e', 'a', 'z', 'j'], ['jalopy', 'zebra', 'entropy', 'avian', 'zoo', 'jape', 'jockey'])
        # lc_list = [LetterCount('e', 1), LetterCount('a', 1), LetterCount('z', 2), LetterCount('j', 3)]
        # self.assertTrue(f1 == lc_list)
